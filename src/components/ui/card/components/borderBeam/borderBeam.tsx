import { BorderBeam } from "@stianlarsen/border-beam";

export const OpenSourceBorderBeam = () => {
  return (
    <BorderBeam size={125} colorFrom="hsl(var(--blue))" colorTo="transparent" />
  );
};
